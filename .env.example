# Growin (Mirae) login for the live orderbook (deno task orderbook).
# Single-session per device: logging in here logs this account out elsewhere.
GROWIN_EMAIL=
GROWIN_PASSWORD=
GROWIN_DEVICE_ID=
GROWIN_PIN=
